{
  "name": "aws-todo-app-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy": "cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --all --force",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "2.174.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "2.174.0",
    "typescript": "~5.7.0",
    "@types/node": "^22.0.0"
  }
}
